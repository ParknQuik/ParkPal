# ParkPal Codex Archive

This archive keeps generic specialist agents available on demand without
loading them through the automatic `.codex/agents` discovery surface.

Restore an archived agent by moving its TOML file from
`.codex/archive/agents/` back into `.codex/agents/`.
