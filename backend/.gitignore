# Environment variables
.env

# Database files
*.db
*.db-journal

# PostgreSQL data (Docker volumes managed externally)
# Migrations are committed, but SQLite backup is ignored
prisma/migrations_sqlite_backup/
